services:
  - type: web
    name: aegis-credit-underwrite
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run_app:app
    envVars:
      - key: FLASK_CONFIG
        value: prod
      - key: SECRET_KEY
        generateValue: true